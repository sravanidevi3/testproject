---
   -
     name: Example playbook
     hosts: all
     become: true

     vars: 
         greeting: Hello World

     tasks:

         - name: Create a directory
           file:
                path: "etc/ansible"
                state: directory

         - name: Deploy greeting
           copy:
                path: "etc/ansible"
                content: "{{ greeting }}"
      
