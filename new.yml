---
   -
     name: Example playbook
     hosts: all
     remote_user: root

     vars: 
         greeting: Hello World

     tasks:

         - name: Create a directory
           file:
                path: "etc/ansible"
                state: directory

         - name: Deploy greeting
           copy:
                dest: "etc/ansible"
                content: "{{ greeting }}"
      
